"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.mudslideFooter = exports.globalOptions = void 0;
exports.getAuthStateCacheFolderLocation = getAuthStateCacheFolderLocation;
exports.initWASocket = initWASocket;
exports.terminate = terminate;
exports.checkLoggedIn = checkLoggedIn;
exports.checkValidFile = checkValidFile;
exports.isLoggedOutDisconnect = isLoggedOutDisconnect;
exports.parseGeoLocation = parseGeoLocation;
exports.waitForKey = waitForKey;
exports.loginWithPairingCode = loginWithPairingCode;
exports.loginWithQrCode = loginWithQrCode;
exports.logout = logout;
exports.onConnectionOpen = onConnectionOpen;
exports.getWhatsAppId = getWhatsAppId;
exports.checkNumberExistsOnWhatsApp = checkNumberExistsOnWhatsApp;
exports.simulateTyping = simulateTyping;
exports.sendSocketMessage = sendSocketMessage;
exports.sendImageHelper = sendImageHelper;
exports.sendFileHelper = sendFileHelper;
exports.handleNewlines = handleNewlines;
const baileys_1 = __importStar(require("baileys"));
const pino_1 = __importDefault(require("pino"));
const path_1 = __importDefault(require("path"));
const fs = __importStar(require("fs"));
const signale_1 = __importDefault(require("signale"));
const os = __importStar(require("os"));
const mime_1 = __importDefault(require("mime"));
const utils_1 = require("./utils");
const QRCode = __importStar(require("qrcode-terminal"));
exports.globalOptions = {
    logLevel: 'silent',
    connectTimeoutMs: 3000,
    defaultQueryTimeoutMs: 6000
};
exports.mudslideFooter = '\u2B50 Please star Mudslide on GitHub! https://github.com/robvanderleek/mudslide';
function getAuthStateCacheFolderLocation() {
    if (process.env.MUDSLIDE_CACHE_FOLDER) {
        return process.env.MUDSLIDE_CACHE_FOLDER;
    }
    else {
        const homedir = os.homedir();
        if (process.platform === 'win32') {
            return path_1.default.join(homedir, 'AppData', 'Local', 'mudslide', 'Data');
        }
        else {
            return path_1.default.join(homedir, '.local', 'share', 'mudslide');
        }
    }
}
function clearCacheFolder() {
    const folder = initAuthStateCacheFolder();
    fs.readdirSync(folder).forEach(f => f.endsWith(".json") && fs.rmSync(`${folder}/${f}`));
}
function initAuthStateCacheFolder() {
    const folderLocation = getAuthStateCacheFolderLocation();
    if (!fs.existsSync(folderLocation)) {
        fs.mkdirSync(folderLocation, { recursive: true });
        signale_1.default.log(`Created mudslide cache folder: ${folderLocation}`);
    }
    return folderLocation;
}
function initWASocket(message) {
    return __awaiter(this, void 0, void 0, function* () {
        const { state, saveCreds } = yield (0, baileys_1.useMultiFileAuthState)(initAuthStateCacheFolder());
        const os = process.platform === 'darwin' ? 'macOS' : process.platform === 'win32' ? 'Windows' : 'Linux';
        const { version } = yield (0, baileys_1.fetchLatestWaWebVersion)({});
        const socket = (0, baileys_1.default)({
            connectTimeoutMs: exports.globalOptions.connectTimeoutMs,
            defaultQueryTimeoutMs: exports.globalOptions.defaultQueryTimeoutMs,
            logger: (0, pino_1.default)({ level: exports.globalOptions.logLevel }),
            auth: state,
            browser: [os, 'Chrome', '10.15.0'],
            version: version,
            syncFullHistory: false,
            getMessage: (_) => __awaiter(this, void 0, void 0, function* () {
                return {
                    conversation: message
                };
            })
        });
        socket.ev.on('creds.update', () => __awaiter(this, void 0, void 0, function* () { return yield saveCreds(); }));
        return socket;
    });
}
function terminate(socket_1) {
    return __awaiter(this, arguments, void 0, function* (socket, waitSeconds = 1) {
        if (waitSeconds > 0) {
            signale_1.default.await(`Closing WA connection, waiting for ${waitSeconds} second(s)...`);
        }
        yield (0, baileys_1.delay)(waitSeconds * 1000);
        socket.end(undefined);
        if (socket.ws && socket.ws.isOpen) {
            yield socket.ws.close();
        }
        console.info(exports.mudslideFooter);
        process.exit();
    });
}
function checkLoggedIn() {
    if (!fs.existsSync(path_1.default.join(initAuthStateCacheFolder(), 'creds.json'))) {
        signale_1.default.error('Not logged in');
        process.exit(1);
    }
}
function checkValidFile(path) {
    if (!(fs.existsSync(path) && fs.lstatSync(path).isFile())) {
        signale_1.default.error(`Could not read image file: ${path}`);
        process.exit(1);
    }
}
function isLoggedOutDisconnect(lastDisconnect) {
    var _a, _b;
    return ((_b = (_a = lastDisconnect === null || lastDisconnect === void 0 ? void 0 : lastDisconnect.error) === null || _a === void 0 ? void 0 : _a.output) === null || _b === void 0 ? void 0 : _b.statusCode) === baileys_1.DisconnectReason.loggedOut;
}
function parseGeoLocation(latitude, longitude) {
    const latitudeFloat = parseFloat(latitude);
    const longitudeFloat = parseFloat(longitude);
    if (isNaN(latitudeFloat) || isNaN(longitudeFloat)) {
        signale_1.default.error(`Invalid geo location: ${latitude}, ${longitude}`);
        process.exit(1);
    }
    return [parseFloat(latitudeFloat.toFixed(7)), parseFloat(longitudeFloat.toFixed(7))];
}
function waitForKey(message) {
    return __awaiter(this, void 0, void 0, function* () {
        signale_1.default.pause(message);
        if (process.stdin.isTTY)
            process.stdin.setRawMode(true);
        return new Promise(resolve => process.stdin.once('data', () => {
            if (process.stdin.isTTY)
                process.stdin.setRawMode(false);
            resolve(undefined);
        }));
    });
}
function loginSecondPass() {
    return __awaiter(this, void 0, void 0, function* () {
        signale_1.default.info('Restart required, logging in again...');
        const socket = yield initWASocket();
        socket.ev.on('connection.update', (update) => __awaiter(this, void 0, void 0, function* () {
            const { connection } = update;
            if (connection === 'open') {
                yield waitForKey("Wait until WhatsApp finishes connecting, then press any key to exit");
                yield terminate(socket);
                signale_1.default.success('Logged in');
            }
        }));
    });
}
function loginWithPairingCode() {
    return __awaiter(this, void 0, void 0, function* () {
        const number = yield (0, utils_1.readPhoneNumber)();
        const socket = yield initWASocket();
        socket.ev.on('connection.update', (update) => __awaiter(this, void 0, void 0, function* () {
            var _a, _b;
            const { connection, lastDisconnect } = update;
            if (connection == "connecting") {
                signale_1.default.await('Waiting 5 seconds before requesting pairing code');
                yield (0, baileys_1.delay)(5000);
                const pairingCode = yield socket.requestPairingCode(number);
                if (pairingCode && pairingCode.length === 8) {
                    signale_1.default.info('In the WhatsApp mobile app go to "Settings > Connected Devices > ');
                    signale_1.default.info('Connect Device" and enter the following pairing code:');
                    signale_1.default.info(pairingCode.substring(0, 4) + '-' + pairingCode.substring(4, 8));
                }
            }
            else if (connection === 'close') {
                if (((_b = (_a = lastDisconnect === null || lastDisconnect === void 0 ? void 0 : lastDisconnect.error) === null || _a === void 0 ? void 0 : _a.output) === null || _b === void 0 ? void 0 : _b.statusCode) === baileys_1.DisconnectReason.restartRequired) {
                    yield loginSecondPass();
                }
                else {
                    signale_1.default.error('Device was disconnected from WhatsApp, use "logout" command first');
                    return;
                }
            }
        }));
    });
}
function loginWithQrCode() {
    return __awaiter(this, void 0, void 0, function* () {
        const socket = yield initWASocket();
        socket.ev.on('connection.update', (update) => __awaiter(this, void 0, void 0, function* () {
            var _a, _b;
            const { connection, lastDisconnect, qr } = update;
            if (qr) {
                signale_1.default.info('In the WhatsApp mobile app go to "Settings > Connected Devices > ');
                signale_1.default.info('Connect Device" and scan the QR code below');
                QRCode.generate(qr, { small: true });
            }
            if (connection === 'close') {
                if (((_b = (_a = lastDisconnect === null || lastDisconnect === void 0 ? void 0 : lastDisconnect.error) === null || _a === void 0 ? void 0 : _a.output) === null || _b === void 0 ? void 0 : _b.statusCode) !== baileys_1.DisconnectReason.loggedOut) {
                    yield loginSecondPass();
                }
                else {
                    signale_1.default.error('Device was disconnected from WhatsApp, use "logout" command first');
                    return;
                }
            }
        }));
    });
}
function logout() {
    return __awaiter(this, void 0, void 0, function* () {
        checkLoggedIn();
        const socket = yield initWASocket();
        let exiting = false;
        socket.ev.on('connection.update', (update) => __awaiter(this, void 0, void 0, function* () {
            const { connection } = update;
            if (exiting) {
                return;
            }
            else if ((update.connection === undefined && update.qr) || connection === 'close') {
                exiting = true;
                clearCacheFolder();
                signale_1.default.success(`Logged out`);
                yield terminate(socket);
            }
            else if (connection === 'open') {
                exiting = true;
                yield socket.logout();
                clearCacheFolder();
                signale_1.default.success(`Logged out`);
                yield terminate(socket);
            }
        }));
        process.on('exit', clearCacheFolder);
    });
}
function onConnectionOpen(socket, onOpen) {
    const onConnectionUpdate = (update) => __awaiter(this, void 0, void 0, function* () {
        const { connection, lastDisconnect } = update;
        if (connection === 'open') {
            socket.ev.off('connection.update', onConnectionUpdate);
            yield onOpen();
        }
        else if (connection === 'close') {
            signale_1.default.error(isLoggedOutDisconnect(lastDisconnect) ? 'Device unlinked from WhatsApp' : 'Connection closed unexpectedly');
            socket.end(undefined);
            process.exit(1);
        }
    });
    socket.ev.on('connection.update', onConnectionUpdate);
}
function getWhatsAppId(socket, recipient) {
    return __awaiter(this, void 0, void 0, function* () {
        if (recipient.startsWith('+')) {
            recipient = recipient.substring(1);
        }
        if (recipient.endsWith('@s.whatsapp.net') || recipient.endsWith('@g.us')) {
            return recipient;
        }
        else if (recipient === 'me') {
            const user = yield socket.user;
            if (user) {
                const phoneNumber = user.id.substring(0, user.id.indexOf(':'));
                return `${phoneNumber}@s.whatsapp.net`;
            }
        }
        return `${recipient}@s.whatsapp.net`;
    });
}
function checkNumberExistsOnWhatsApp(socket, whatsappId) {
    return __awaiter(this, void 0, void 0, function* () {
        var _a;
        const result = yield socket.onWhatsApp(whatsappId);
        return !!((_a = result === null || result === void 0 ? void 0 : result[0]) === null || _a === void 0 ? void 0 : _a.exists);
    });
}
function simulateTyping(socket, whatsappId, ms) {
    return __awaiter(this, void 0, void 0, function* () {
        yield socket.sendPresenceUpdate('composing', whatsappId);
        yield (0, baileys_1.delay)(ms);
        yield socket.sendPresenceUpdate('paused', whatsappId);
    });
}
function sendSocketMessage(socket_1, whatsappId_1, payload_1) {
    return __awaiter(this, arguments, void 0, function* (socket, whatsappId, payload, options = {}) {
        if (options.liveCheck) {
            const exists = yield checkNumberExistsOnWhatsApp(socket, whatsappId);
            if (!exists) {
                signale_1.default.error(`Recipient does not exist on WhatsApp: ${whatsappId}`);
                socket.end(undefined);
                process.exit(1);
            }
        }
        if (options.typing) {
            yield simulateTyping(socket, whatsappId, options.typing);
        }
        yield socket.sendMessage(whatsappId, payload);
        signale_1.default.success('Done');
        yield terminate(socket, 3);
    });
}
function sendImageHelper(socket, whatsappId, filePath, options) {
    return __awaiter(this, void 0, void 0, function* () {
        const payload = { image: fs.readFileSync(filePath), caption: handleNewlines(options.caption) };
        yield sendSocketMessage(socket, whatsappId, payload, options);
    });
}
function sendFileHelper(socket, whatsappId, filePath, options) {
    return __awaiter(this, void 0, void 0, function* () {
        const payload = {
            mimetype: mime_1.default.getType(filePath),
            caption: handleNewlines(options.caption)
        };
        switch (options.type) {
            case "audio":
                payload['audio'] = fs.readFileSync(filePath);
                break;
            case "video":
                payload['video'] = fs.readFileSync(filePath);
                break;
            default:
                payload['document'] = fs.readFileSync(filePath);
                payload['fileName'] = path_1.default.basename(filePath);
        }
        yield sendSocketMessage(socket, whatsappId, payload, options);
    });
}
function handleNewlines(s) {
    if (s) {
        return s.replace(/\\n/g, '\n');
    }
}
